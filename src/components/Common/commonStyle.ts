import { styled } from "styled-components";
import { BaseContainer, ContentWrapper } from "./BaseContainer";

export const StyledBaseContainer = styled(BaseContainer)`
${ContentWrapper} {
  padding: 0 0;
}
`;
